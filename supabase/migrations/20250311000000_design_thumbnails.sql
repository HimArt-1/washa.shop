-- ═══════════════════════════════════════════════════════════
--  وشّى | WASHA — صور التعديلات + مصغرات متعددة
-- ═══════════════════════════════════════════════════════════

-- صور التصميم بعد التعديل (للمقارنة)
ALTER TABLE custom_design_orders ADD COLUMN IF NOT EXISTS modification_design_url TEXT;
